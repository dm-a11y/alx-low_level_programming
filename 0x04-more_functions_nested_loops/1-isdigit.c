#include "main.h"

/**
 * function -isdigit to check whether the value passed to it are digits or  * not.
 */
int _isdigit(int c)
{
	if (c = 48 && c <= 57)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
