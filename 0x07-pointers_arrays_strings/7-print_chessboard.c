#include "main.h"

/**
 * print_chessboard - prints chessboard
 * @: pointer to a 2d array
 *
 * Return: Nothing
 */
void print_chessboard(char (*a)[8])
{
	int x, y;

	for (x = 1; x <= 8; x++)
	{
		for (y = 0; y < 8; y++)
			_putchar(a[0][y];
				a++;
	}
	_putchar('\n');
}
